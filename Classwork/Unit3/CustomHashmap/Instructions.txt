Find a data set which requires continuous access to its elements using non-index keys with at least 500 elements.

Create a hashcode function that minimizes collision and demonstrate that fewer than 30% collisions occur in this data set. The function must be easy enough to perform by a human with a calculator. Don’t use more than 2 data fields for the key.
Use Key, Value Entry classes to organize the data in the HashMap
The CustomHashMap will have a private array of  tables of buckets and the following methods
put(Entry<Key,Value>)
get(Key)
remove(Key)
rehash()//triggers both higher at lower at .75 and .10
toString (will provide a summary of the elements and load factor)
loadFromFile(File)
loadFactor()
put(i, Entry<Key,Value>)  //places the Entry into a specific spot to demonstrate collision
Key Class needs to have
equals(Key)
hashCode()
The collisions will be handled with a linked list
Create A Driver that will load in the file and have an interface for the user to get, put, and remove elements from the data base. If the put creates a collision OUTPUT THAT ON THE ON SCREEN.
The user can decide if the put can also load from another file
Free Data Sources in CSV
https://www.kaggle.com/datasets

https://github.com/awesomedata/awesome-public-datasets

https://www.gapminder.org/data/
